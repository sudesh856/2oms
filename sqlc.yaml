version: "2"

sql:
  - engine: "postgresql"
    queries: "backend/internal/db/queries"
    schema: "database/migrations"
    gen:
      go:
        package: "db"
        out: "backend/internal/db/generated"
        sql_package: "pgx/v5"

